#include "binary_trees.h"
/**
 * array_to_heap: creates a heap
 * @array: used in referencing array
 * @size: denotes the size
 * Return: points
 */
heap_t *array_to_heap(int *array, size_t size)
{
	return (NULL);
}
