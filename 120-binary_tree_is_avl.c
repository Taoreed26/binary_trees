#include "binary_trees.h"
/**
 * binary_tree_is_avl: evaluates if it is AVL
 * @tree: what is expected to be evaluated
 * Return: returns
 */
int binary_tree_is_avl(const binary_tree_t *tree)
{
	return (1);
}
