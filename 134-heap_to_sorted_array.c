#include "binary_trees.h"
/**
 * heap_to_sorted_array: used to convert binary map to sorted array
 * @heap: works on the root
 * @size: cannotes how big or small the array is
 * Return: points
 */
int *heap_to_sorted_array(heap_t *heap, size_t *size)
{
	return (NULL);
}
