#include "binary_trees.h"
/**
 * array_to_avl:this is a function used for creating
 * @array: array with nodes
 * @size: sizes the array
 * Return: points
 */
avl_t *array_to_avl(int *array, size_t size)
{
	return (NULL);
}
