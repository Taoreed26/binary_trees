#include "binary_trees.h"
/**
 * binary_tree_is_heap: checker for the heap tree
 * @tree:evaluates te root
 * Return: returns high or low
 */
int binary_tree_is_heap(const binary_tree_t *tree)
{
	return (1);
}
