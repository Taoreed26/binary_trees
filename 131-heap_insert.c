#include "binary_trees.h"
/**
 * heap_insert: functions as an insertion key
 * @root:evaluates the root
 * @value: relates the insertion
 * Return: points
 */
heap_t *heap_insert(heap_t **root, int value)
{
	return (NULL);
}
