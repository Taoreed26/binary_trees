#include "binary_trees.h"
/**
 * sorted_array_to_avl: a function used for creating
 * @array: used for evaluation
 * @size: search engine
 * Return: points
 */
avl_t *sorted_array_to_avl(int *array, size_t size)
{
	return (NULL);
}
