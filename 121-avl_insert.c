#include "binary_trees.h"
/**
 * avl_insert: inserts node
 * @tree: this is the root to be evaluated
 * @value: inserts Node
 * Return: this serves as a pointer
 */
avl_t *avl_insert(avl_t **tree, int value);
{
	return (NULL);
}
