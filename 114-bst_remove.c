#include "binary_trees.h"
/**
 * bst_remove: removes the necessary 
 * @root: evaluates the root
 * @value: finds the node
 * Return: returns the pointer
 */
bst_t *bst_remove(bst_t *root, int value)
{
	return (NULL);
}
