#include "binary_trees.h"
/**
 * avl_remove: removal function
 * @root: the tree root
 * @value: used as a search engine
 * Return: points
 */
avl_t *avl_remove(avl_t *root, int value)
{
	return (NULL);
}
